<h1>Editing Book</h1>
<%= form_with model:@book, local:true do |f| %>
  <label>Title</label>
  <%= f.text_field :title, autofocus: true %>
  <label>Option</label>
  <%= f.text_field :body %>
  <%= f.submit "Update Book" %>
<% end %>